using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MakeAMacroButton : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void OnTriggerEnter () {
		//instantiate on the end of the other hand 
		//Instantiate(aMacroButton);
		// if wand clicked leave the macro there
		//switch to macroedit mode
		//connect type keys to macro
		//take the first letters till space for lable
		//put all the letters in a string for use later



	}
}
